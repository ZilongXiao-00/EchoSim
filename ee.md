1. 新增的「画像存储 + 画像召回」能力，**全部封装成「自定义工具」**，挂载到原有 Agent 上，复用你熟悉的 `config.tools` 配置逻辑；

2.向量数据库操作，全部在项目本地执行，数据文件存在项目目录内，完全本地化闭环。

3.保持原有 `Agent A/B/C/D` 角色不变、原有 7 个环节不变、原有频道消息规则不变；

4.新增步骤，画像自动入库（在「环节 3 画像生产」之后执行）

5.新增步骤，画像精准召回（在「环节 1 需求发起」之后，环节 2 架构设计之前执行）

（直接复用 Agent A，给它新增一个「画像召回工具」即可，无需新增其他 Agent）

（Agent A 自动解析用户的新需求关键词（比如「大学生」「咖啡」「消费偏好」）→ 调用「召回工具」查询**本地 Chroma 向量库** → 精准匹配出「相似画像」（比如：手机调研里的「艾米莉・张 - 21 岁 - 经济学 - 精打细算」）→ 将召回的画像 ID / 画像信息，自动追加到新调研的 `target_audience` 中）

（衔接原流程，Agent A 继续执行原有逻辑，发送 `[SURVEY_TASK_INIT]` 到`#persona-db`，只是此时的 JSON 里多了「复用画像」的信息）



```bash
pip install chromadb sentence-transformers openagents python-dotenv
```